version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.uat
    image: jackson-app:uat
    container_name: jackson-app-uat
    restart: always
    ports:
      - "8000:8000"
    networks:
      - sre-net

networks:
  sre-net:
    external: true
